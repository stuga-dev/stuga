# Only the unix socket exists, in an owner-only directory. peer asks the kernel who is
# connecting and admits only the OS user pg_ident.conf maps: there is no password.
#
# TYPE  DATABASE  USER  METHOD
local   all       all   peer map=stuga
