# The one OS user that may act as the `stuga` role: the account that runs Postgres, the
# node and every stuga-node command. init-cluster.sh fills @OS_USER@.
#
# MAPNAME  SYSTEM-USERNAME  PG-USERNAME
stuga      @OS_USER@        stuga
