{
  "name": "@stuga/db",
  "version": "0.0.1",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "schema:snapshot": "vitest run schema-snapshot -u"
  },
  "dependencies": {
    "@stuga/protocol": "workspace:*",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@stuga/auth": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
